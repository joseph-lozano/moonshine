<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      <%= post.title %>
    </title>
    <meta charset="UTF-8"/>
    <%= if post.description != "" do %>
      <meta name="description" content="<%= post.description %>"/>
    <% end %>
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>
    <link rel="stylesheet" href="/styles.css"/>
    <link rel="stylesheet" href="/elixir.css" >
    <%# <script src="/js/alpine.js?v={% version %}"></script> %>
  </head>
  <body>
    <%= render.("site/templates/_header.html.eex", []) %>
    <div class="relative py-16 bg-white overflow-hidden">
      <div class="relative px-4 sm:px-6 lg:px-8">
        <div class="text-lg max-w-prose mx-auto">
          <%= if post.draft do %>
            <div class="bg-yellow-300 text-center">DRAFT</div>
          <% end %>
          <h1>
            <span class="mt-2 block text-4xl text-center leading-8 font-extrabold tracking-tight text-gray-900 sm:text-4xl">
              <%= post.title %>
            </span>
          </h1>
          <%= if post.date do %>
            <div class="mt-1 text-md text-center text-gray-400">
              Published <%= post.date %>
            </div>
          <% end %>
        </div>
        <div class="mt-6 prose prose-primary prose-md text-gray-700 mx-auto">
          <%= inner_content %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
